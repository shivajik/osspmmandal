# Python deps used by the Vercel serverless function at /api/index.py.
# Keep this list minimal — only what /backend/server.py needs at runtime.

fastapi==0.110.1
pydantic>=2.6.4
email-validator>=2.2.0
python-dotenv>=1.0.1
motor==3.3.1
pymongo==4.5.0
starlette>=0.36.0
